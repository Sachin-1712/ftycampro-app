<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device. Camera credentials are held in EncryptedSharedPreferences
  keyed by the hardware keystore, so a backup would be useless anyway, but excluding
  them explicitly avoids ever writing them to cloud backup or a device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="database" path="." />
        <exclude domain="file" path="." />
    </device-transfer>
</data-extraction-rules>
