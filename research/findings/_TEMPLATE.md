# Finding NN: <one-line claim>

**Date:**
**Status:** confirmed | superseded | retracted
**Confirms / kills hypothesis:** H_

## Claim

One or two sentences. State exactly what is now known — no hedging, and no more
than the evidence supports.

## How it was shown

The command, with its actual output. Someone else on the same network should be
able to paste this and get the same result.

```
$ python tools/p2p_probe.py --target 192.168.29.214 --broadcast 192.168.29.255
...
```

## Evidence

Hex dumps, packet excerpts, decompiled snippets. Redact UIDs, tokens and Wi-Fi
passphrases — replace with `<UID>`, `<TOKEN>` and note that you did.

## What this rules out

The alternatives this eliminates, and why. A finding that doesn't narrow anything
is a note, not a finding.

## What it doesn't show

The boundary of the claim. This section is what stops the finding from being
over-read later.

## Next

The specific step this unblocks.
